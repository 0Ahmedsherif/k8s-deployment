pre-requisits to connect to the cluster

> install aws cli
> install aws-iam-authenticator
> install kubectl with the same version of the cluster

# aws eks update-kubeconfig --name ${cluster_name} --region ${region}
